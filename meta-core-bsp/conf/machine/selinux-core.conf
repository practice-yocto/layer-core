# -----------------------------------------------------------------------------
# Include the base machine configuration
# This pulls in the default settings, hardware definitions, and kernel options
# for the 'core' machine family.
# -----------------------------------------------------------------------------
require conf/machine/systemd-core.conf

# Enable SELinux-specific machine support. Distro policy and user space are
# selected by the distro configuration.
MACHINE_FEATURES:append = " selinux"
